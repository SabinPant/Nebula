/**
 * PostCSS Configuration
 *
 * Processes CSS through Tailwind (utility class generation)
 * and Autoprefixer (vendor prefixes for browser compatibility).
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};