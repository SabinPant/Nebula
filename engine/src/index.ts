/**
 * Engine Entry Point
 *
 * Placeholder for the Market Simulation Engine.
 * The real engine (GBM pricing, order matching, circuit breaker, market state machine)
 * will be built in Sprint 9–10. Until then, the server uses an internal mock engine.
 *
 * This file exists to satisfy the monorepo workspace — it does nothing yet.
 */

async function main(): Promise<void> {
  console.log('Nebula Engine — placeholder (Sprint 9)');
  process.exit(0);
}

main();